myMode = ' '
if myMode != 'q':
    myMode =  input("Would you like to encrypt or decrypt the message?>")
    if myMode != 'q':
        print("You chose" , myMode)
    else:
        exit()
    
"""this program allows the user to choose either encryption or decryption."""
